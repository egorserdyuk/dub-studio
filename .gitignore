__pycache__/
*.pyc
*.egg-info/
workspace/
casting_library/
node_modules/
dist/
test_e2e_*

# portable runtime — all created by install.bat / run.bat, never committed
python/
.venv-sortformer/
node/
ffmpeg/
models/
voices/
downloads/
temp/
cache/
output/
cuda_version.txt
run-dev.bat

# Rust-порт (crates/): артефакты сборки; веса/бинари движков не коммитим
/target/
target/
Cargo.lock
*.dll

# llama.cpp prebuilt-бинари (сайдкар llama-server) — негитуемо, кладёт раунд 3
tools/


# Tauri auto-generated schemas
desktop/src-tauri/gen/

# Tauri bundle staging — генерируется перед `npx tauri build` (dub-server.exe, frontend/dist,
# fonts, bundled VC++/OCR); см. desktop/src-tauri/STAGING.md. Артефакт сборки, не коммитим.
desktop/src-tauri/staging/
target2/
scratchpad/
logs/
demos/

# тест-видео кастинга (#115) — НЕ в репозиторий (419МБ), только на диске для E2E
frontend/public/*.mp4

# тестовые видео — не в репо (>100МБ лимит GitHub)
test_media/

# git worktrees для изолированной разработки (superpowers) — не коммитим
.worktrees/
